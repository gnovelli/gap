application.title=QAGESA
application.vendor=Giovanni Novelli
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/MC.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.MC-src=../../MC/src
file.reference.QAGESA-src=../../trunk/QAGESA/src
includes=**
jar.compress=false
javac.classpath=\
    ${libs.junit_4.classpath}:\
    ${libs.JavaCSV.classpath}:\
    ${reference.GridSim.jar}:\
    ${reference.GAP.jar}:\
    ${libs.SimJava2.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jnlp.codebase.type=local
jnlp.codebase.url=file:/C:/Users/giovanni/Documents/Dottorato/GAPdev/trunk/netbeans/QAGESA/dist/
jnlp.enabled=false
jnlp.offline-allowed=true
jnlp.signed=true
# Property libs.JavaCSV.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JavaCSV.classpath=../../external/javacsv2.0/javacsv.jar
# Property libs.SimJava2.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.SimJava2.classpath=../../external/gridsim/trunk/jars/simjava2.jar
main.class=net.sf.gap.mc.EXPERIMENTS
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
project.GAP=../GAP
project.GridSim=../GridSim
reference.GAP.jar=${project.GAP}/dist/GAP.jar
reference.GridSim.jar=${project.GridSim}/dist/GridSim.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Xms1024m -Xmx1024m
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.src.dir=${file.reference.MC-src}
src.xml.dir=xml
test.src.dir=test
